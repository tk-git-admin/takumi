# Agent Instructions

## Reusable Skills

Reusable implementation skills live in `docs/skills`.

Before making non-trivial implementation changes, inspect `docs/skills` for a matching `SKILL.md`. If a skill matches the task, follow it unless project-specific instructions conflict.

After cloning this repository, initialize skills with:

```sh
git submodule update --init --recursive
```
